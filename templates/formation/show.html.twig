{% extends 'base.html.twig' %}

{% block titre %}Formation / {{ formation.nomCourt }} {% endblock %}

{% block contenu %}
    <h2>Formation</h2>

    <table class="table table-striped table-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formation.id }}</td>
            </tr>
            <tr>
                <th>Nom court</th>
                <td>{{ formation.nomCourt }}</td>
            </tr>
            <tr>
                <th>Nom long</th>
                <td>{{ formation.nomLong }}</td>
            </tr>
        </tbody>
    </table>

    
    <br>
    <a class="btn btn-danger" href="{{ path('formation_edit', {'slug': formation.slug}) }}" role="button" > Editer cette formation </a>
    <br><br>

    {{ include('formation/_delete_form.html.twig') }}
    <br><br>
    
    <a class="btn btn-outline-warning" href="{{ path('formation_index') }}">Retour à la liste des formations</a>
{% endblock %}
